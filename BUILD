

remember to generate a new classpath for the start script.

mvn dependency:build-classpath -DincludeScope=runtime

should resolve this by building the artifact with all dependencies.  Actually, this didn't work because of incorrect classpath ordering and clobbering of configuration files.




